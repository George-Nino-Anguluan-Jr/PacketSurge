{
  "challenge_id": "search_binary",
  "topic": "search_binary",
  "difficulty": "medium",
  "types": ["fill_blank", "fix_syntax", "fix_and_optimize"],
  "challenges": {
    "fill_blank": [
      {
        "id": "search_binary_fill_1",
        "prompt": "Calculate middle index in binary search.",
        "template": "def binary_search(arr, target):\n    left, right = 0, len(arr) - 1\n    while left <= right:\n        mid = (left + right) // ___\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1",
        "answer": "2",
        "explanation": "Integer division by 2 gives middle index.",
        "hints": ["Integer division", "Floor division by 2"]
      },
      {
        "id": "search_binary_fill_2",
        "prompt": "Move left bound when target is in right half.",
        "template": "elif arr[mid] < target:\n    left = ___",
        "answer": "mid + 1",
        "explanation": "Target is right of mid, so search right half.",
        "hints": ["Exclude mid", "Search right side"]
      }
    ],
    "fix_syntax": [
      {
        "id": "search_binary_fix_1",
        "prompt": "Fix - infinite loop (wrong bound update):",
        "code": "def binary_search(arr, target):\n    left, right = 0, len(arr) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            left = mid\n        else:\n            right = mid\n    return -1",
        "answer": "def binary_search(arr, target):\n    left, right = 0, len(arr) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1",
        "explanation": "Must use mid + 1 and mid - 1 to exclude checked element.",
        "hints": ["Exclude mid from next search", "mid + 1 and mid - 1"]
      }
    ],
    "fix_and_optimize": [
      {
        "id": "search_binary_opt_1",
        "prompt": "Optimize - use bisect module:",
        "code": "def binary_search(arr, target):\n    left, right = 0, len(arr) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1",
        "answer": "import bisect\ndef binary_search(arr, target):\n    i = bisect.bisect_left(arr, target)\n    if i != len(arr) and arr[i] == target:\n        return i\n    return -1",
        "explanation": "bisect module is implemented in C, faster and battle-tested.",
        "hints": ["bisect.bisect_left", "Returns insertion point"]
      }
    ]
  }
}